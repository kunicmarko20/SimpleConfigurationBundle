{% extends admin.getTemplate('base_list_field') %}

{% block field %}
    {% spaceless %}
        {% if value is iterable %}
            {% set value = value|join(', ') %}
        {% endif %}
        {{ value }}
    {% endspaceless %}
{% endblock %}
